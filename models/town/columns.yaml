# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    name:
        label: Name
        searchable: true
    state:
        label: State
        relation: state
        select: name
    slug:
        label: URL
        searchable: true
    excerpt:
        label: Excerpt
    is_enabled:
        label: Enabled
        type: switch
